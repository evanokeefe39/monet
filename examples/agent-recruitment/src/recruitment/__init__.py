"""Agent recruitment + performance-management example.

This package ships two reference capability agents (``code_executor``,
``data_analyst``) and one after_agent hook (``record_run_summary``) that
together let monet's default planner compose three end-to-end use cases:
discovery, trial, and performance management. See ``README.md``.
"""
