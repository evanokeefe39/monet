[project]
name = "monet-quickstart-local"
version = "0.0.0"
description = "Quickstart local — single-process content workflow with monet."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../..", editable = true }
