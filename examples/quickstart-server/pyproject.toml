[project]
name = "monet-quickstart-server"
version = "0.0.0"
description = "Quickstart server — client/server content workflow with monet."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../..", editable = true }
