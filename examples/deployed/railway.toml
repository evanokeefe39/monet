[build]
builder = "NIXPACKS"

[deploy]
startCommand = "aegra serve --host 0.0.0.0 --port ${PORT:-2026}"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
