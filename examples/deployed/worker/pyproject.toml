[project]
name = "monet-deployed-worker"
version = "0.0.0"
description = "Railway/self-host — monet worker claiming tasks from a remote server."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../../..", editable = true }
