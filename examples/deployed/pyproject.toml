[project]
name = "monet-deployed"
version = "0.0.0"
description = "Railway deployment — monet server with managed Postgres + Langfuse."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../..", editable = true }
