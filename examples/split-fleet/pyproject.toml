[project]
name = "monet-split-fleet"
version = "0.0.0"
description = "Multi-pool worker fleet — one server, N workers per pool (S3)."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../..", editable = true }
