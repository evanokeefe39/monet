[project]
name = "monet-local"
version = "0.0.0"
description = "Local development — Docker Compose with Postgres + Langfuse."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../..", editable = true }
