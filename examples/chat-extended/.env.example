# Required: at least one LLM provider
GEMINI_API_KEY=
GROQ_API_KEY=

# PostgreSQL — must match the credentials baked into .monet/docker-compose.yml.
DATABASE_URL=postgresql://chat_extended:chat_extended_secret@localhost:5432/chat_extended
