[project]
name = "monet-quickstart"
version = "0.0.0"
description = "Quickstart — content workflow with monet dev + monet run."
requires-python = ">=3.12"
dependencies = [
    "monet[examples]",
]

[tool.uv.sources]
monet = { path = "../..", editable = true }
