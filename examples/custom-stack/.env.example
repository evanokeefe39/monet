# The custom stack uses a deterministic stub LLM — no API keys required.
# These vars are only here so monet/aegra's boot checks pass.

GEMINI_API_KEY=stub
DATABASE_URL=postgresql://custom_stack:custom_stack_secret@localhost:5432/custom_stack
