GEMINI_API_KEY=
GROQ_API_KEY=

# PostgreSQL — must match the credentials baked into .monet/docker-compose.yml.
DATABASE_URL=postgresql://custom_pipeline:custom_pipeline_secret@localhost:5432/custom_pipeline
