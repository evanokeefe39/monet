"""Pipeline adapters — multi-graph compositions driven by ``MonetClient``.

A *pipeline* is a sequence or DAG of graphs orchestrated with typed
HITL gates, state propagation between graphs, and domain events
projected from the generic core events. See
:mod:`monet.pipelines.default` for the built-in
entry → planning → execution pipeline.
"""
