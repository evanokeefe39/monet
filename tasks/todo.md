# Production-Readiness Plan

Architecture review (`architecture-checks`) surfaced 16 high-severity findings. Queue ADR v3 (commits `984db6d..8ed402d`) closed most queue-internal ones. Remaining blockers + pre-prod hygiene tracked below.

Out of scope per user clarification:
- RR-30 agent idempotency — agent-implementation concern
- DO-50 token/cost capture — agent emits Langfuse directly, black-box contract

## Phase 0 — ST-04 ArtifactPointer key round-trip (blocker)

Queue refactor preserved the bug. `src/monet/core/_serialization.py:64-67` reconstructs only `artifact_id`+`url`; drops `key`. Breaks `find_artifact(..., "work_brief")` when result crosses queue path (all S2/S3).

- [ ] Extract shared `ArtifactPointer` codec helper (likely `types.py` or `_serialization.py`) so `_routes.py:_build_artifact_pointer` and `deserialize_result` share one path
- [ ] Fix `deserialize_result` to preserve `key` (and any future optional fields)
- [ ] `tests/test_serialization.py::test_artifact_pointer_key_round_trip` — assert `key` survives serialize→deserialize
- [ ] `tests/test_serialization.py::test_artifact_pointer_without_key_round_trip` — optional-absent case
- [ ] Verify via existing `tests/test_default_pipeline_events.py` that fakes path still passes
- [ ] Commit

## Phase A1 — alembic scaffold (RR-22)

`_deployment.py` + `_index.py` use `CREATE TABLE IF NOT EXISTS` / `Base.metadata.create_all`. Schema changes silently skip new columns. Must land before A2 adds indexes.

- [ ] Add `alembic` to `[dependency-groups] dev` in pyproject.toml
- [ ] Create `migrations/` directory with `env.py`, `script.py.mako`, empty `versions/`
- [ ] Baseline migration `0001_baseline.py` — captures current `artifacts` schema (autogenerate from `ArtifactRecord`) + `deployments` schema (hand-written raw SQL mirroring `_deployment.py:44-58`)
- [ ] Replace `SQLiteIndex.initialise` (`Base.metadata.create_all`) and `DeploymentStore.initialize` (raw CREATE TABLE) with a startup check — if alembic head not applied, raise `ConfigError` with actionable message naming `monet db migrate`
- [ ] New CLI entry `monet db migrate` + `monet db current` in `src/monet/cli/_db.py`; register in `cli/__init__.py`
- [ ] Test: fresh DB → `monet db migrate` → all tables + indexes present
- [ ] Test: existing DB without alembic state → stamp head works, no data loss
- [ ] Test: boot fails cleanly when migrations pending
- [ ] Update `docs/reference/env-vars.md` + `docs/guides/distribution.md` with migration workflow
- [ ] Update CLAUDE.md `## Layout` to include `migrations/` + `cli/_db.py`
- [ ] Commit

## Phase A2 — artifact index secondary indexes (DA-53)

`query_by_run` is the only query method and runs full-scan. Needed before any prod query workload.

- [ ] Add `Index(...)` declarations to `ArtifactRecord.__table_args__`:
  - `ix_artifacts_run_id` on `run_id`
  - `ix_artifacts_agent_id` on `agent_id`
  - `ix_artifacts_trace_id` on `trace_id`
  - `ix_artifacts_run_created` on `(run_id, created_at)` composite
- [ ] Alembic revision `0002_artifact_indexes.py` — autogenerated
- [ ] Test: `EXPLAIN QUERY PLAN` shows index usage on `query_by_run`
- [ ] Commit

## Phase A3 — pre-prod hygiene (parallel batch)

Each item is a small independent commit.

- [ ] `.dockerignore` at repo root — exclude `.git`, `__pycache__`, `.venv`, `tests/`, `docs/`, `examples/*/.monet/`, `*.log`, `node_modules`, `uv.lock` intentionally kept
- [ ] Delete `src/monet/descriptors.py` + `tests/test_descriptors.py` — verify zero src consumers first; update CLAUDE.md Layout to remove the reference
- [ ] Centralize `os.environ` in reference agents — 5 agents + `cli/_setup.py` + `cli/_worker.py` use direct env reads; replace with `config._env.agent_model_env()` (helper exists, unused) and the `Final` constants for API keys
- [ ] Dockerfile `HEALTHCHECK` on monet service Dockerfiles (`examples/deployed/server/`, `examples/split-fleet/compose/`) — hits `/health` which now real-probes Redis
- [ ] `.github/workflows/security.yml` — `pip-audit` weekly + on-PR; `.github/dependabot.yml` for pip + github-actions
- [ ] Image SHA tags in compose files — replace `monet-split-fleet:latest` with `monet-split-fleet:${GIT_SHA:-dev}`; document `GIT_SHA` in example READMEs
- [ ] `uv sync --frozen` in deployed Dockerfiles (instead of `uv pip install -e .`) — reproducible builds from `uv.lock`
- [ ] Pin base image digests + weekly bump workflow — `python:3.12-slim-bookworm@sha256:<digest>` + `.github/workflows/digest-bump.yml` opening PR weekly

## Review

(add after completion)
